<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gopenang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoPenang">

        <activity android:name=".signUp" />
        <activity android:name=".logIn" />
        <activity android:name=".bottomNavi" />
        <activity android:name=".editProfile" />
        <activity android:name=".feedback" />
        <activity android:name=".forgotPw" />
        <activity android:name=".changePassword" />
        <activity android:name=".termsPolicy" />
        <activity android:name=".delacc" />
        <activity android:name=".aboutFood" />
        <activity android:name=".aboutPlaces" />
        <activity android:name=".content" />
        <activity android:name=".aboutPenang" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>